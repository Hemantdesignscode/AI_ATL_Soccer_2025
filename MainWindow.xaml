<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Coaching.ai.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Coaching.ai"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Coaching.ai">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TitleBar Grid.Row="0" Title="MyCoach" x:Name="title_replace">
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/soccerball_icon.png"/>
            </TitleBar.IconSource>
        </TitleBar>

        <Image Grid.Row="2" Source="/Assets/Gemini_Generated_Image_rx26flrx26flrx26.png" Stretch="Fill"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="10">
            <DropDownButton Content="Formation">
                <DropDownButton.Flyout>
                    <MenuFlyout>
                        <MenuFlyoutSubItem Text="Players">
                            <MenuFlyoutItem Text="Show Stats"/>
                            <MenuFlyoutItem Text="Add Player/Athlete"/>
                            <MenuFlyoutItem Text="Delete Player/Athlete"/>
                        </MenuFlyoutSubItem>
                        <MenuFlyoutSubItem Text="Formations">
                            <MenuFlyoutItem Text="4-4-2"/>
                            <MenuFlyoutItem Text="4-3-3"/>
                            <MenuFlyoutItem Text="4-5-1"/>
                            <MenuFlyoutItem Text="4-2-3-1"/>
                            <MenuFlyoutItem Text="4-1-4-1"/>
                        </MenuFlyoutSubItem>
                    </MenuFlyout>
                </DropDownButton.Flyout>
            </DropDownButton>
            <Button Content="Final"></Button>
        </StackPanel>
    </Grid>



</Window>